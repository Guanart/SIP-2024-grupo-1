apiVersion: v1
kind: ConfigMap
metadata:
    name: backend-config
data:
    DATABASE_URL: 'postgresql://postgres:admin@postgres:5432/LOT'
    AUTH0_ISSUER_URL: https://dev-f57qs7dbi1xcl5kj.us.auth0.com/
    AUTH0_AUDIENCE: http://my-secure-api.com
    PORT: 3000
    APP_URL: 'https://<IP_BACKEND_SERVICE>:3000'
    REACT_APP_URL: 'http://<IP_FRONTEND_SERVICE>'
    MP_APP_ID=: 1517187722603608" # sandbox
    MP_ACCESS_TOKEN: '' # usar el de la cuenta sandbox
    MP_CLIENT_SECRET: '' # usar el de la cuenta sandbox
    MP_REDIRECT_URI: 'https://<IP_BACKEND_SERVICE>:3000/mercado-pago/oauth'
---
apiVersion: v1
kind: ConfigMap
metadata:
    name: frontend-config
data:
    APP_BACKEND_HOST: 'backend'
    APP_BACKEND_PORT: '3000'
    APP_AUTH0_AUDIENCE: 'http://my-secure-api.com'
    APP_AUTH0_DOMAIN: 'dev-f57qs7dbi1xcl5kj.us.auth0.com'
    APP_AUTH0_CLIENT_ID: 'QDUde2yWkQWxGguu7p59G3QirNNpeXgl'
---
apiVersion: v1
kind: ConfigMap
metadata:
    name: postgres-config
    labels:
        app: postgres
data:
    POSTGRES_DB: LOT
    POSTGRES_USER: postgres
    POSTGRES_PASSWORD: admin
